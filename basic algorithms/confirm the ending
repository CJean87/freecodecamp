
function confirmEnding(str, target) {
  var targetLength = target.length;
  var strSub = str.substring(str.length - targetLength);
  if (strSub === target) {
    return true;
  }else{
    return false;
  }
}

confirmEnding("Bastian", "n");
